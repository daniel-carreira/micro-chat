steps:
- name: 'gcr.io/cloud-builders/docker-compose'
  args: ['up', '--build']
  timeout: '1600s'
  env:
    - 'COMPOSE_PROJECT_NAME=micro-chat'
    - 'COMPOSE_FILE=docker-compose.yaml'
options:
  logging: 'NONE'
serviceAccount: 'user-201@micro-chat-382821.iam.gserviceaccount.com'